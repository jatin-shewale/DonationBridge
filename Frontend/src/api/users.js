import client from "./client.js";

// Placeholder module kept for symmetry with the API structure described in the spec;
// donor/NGO/admin user management currently goes through auth.js and ngos.js admin endpoints.
export { getMe, updateMe } from "./auth.js";
